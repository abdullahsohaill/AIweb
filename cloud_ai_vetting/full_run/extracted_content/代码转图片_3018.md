# 代码转图片
**URL:** http://carbon.now.sh
**Page Title:** Carbon | Create and share beautiful images of your source code
--------------------

### (Raw Extraction Fallback)

Create and share beautiful images of your source code.
Start typing or drop a file into the text area to get started.
Sign in/up
Theme
Language
Background Menu
Settings Menu
Copy menu
Tweet
Share menu dropdown
Quick export
Export
Export menu dropdown
const pluckDeep = key => obj => key.split('.').reduce((accum, key) => accum[key], obj)
​
const compose = (...fns) => res => fns.reduce((accum, next) => next(accum), res)
​
const unfold = (f, seed) => {
  const go = (f, seed, acc) => {
    const res = f(seed)
    return res ? go(f, res[1], acc.concat([res[0]])) : acc
  }
  return go(f, seed, [])
}
Check out the latest remote job listings from the leading job board for designers, developers, and creative pros.
ADS VIA CARBON
aboutsourcetermsprivacyoffsets
created by @carbon_app ¬

--------------------